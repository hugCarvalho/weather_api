import React from "react";
import { render } from "@testing-library/react";
import DisplayCityName from "./DisplayCityName";

test("should ", () => {
  render(<DisplayCityName validCity="mocking" isLoading={false} />);
});
